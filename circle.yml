machine:
  services:
    - docker
  node:
    version: 4.0.0

deployment:
  hub:
    branch: master
    commands:
      - docker build -t utilitywarehouse/uw-echo .
      - docker login -e "$DOCKER_EMAIL" -u "$DOCKER_USER" -p "$DOCKER_PASS"
      - docker push utilitywarehouse/uw-echo
